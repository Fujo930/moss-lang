type A = 
  x: Number

type B =
  x: Number

// A and B have same shape — checker may confuse them
fn useA(v: A) {
  print(v.x)
}

let val: B = { x: 42 }
useA(val)  // structural typing: is this caught?
